// Eingabewert wird übernommen und in eine Zahl konvertiert
// anschließend wird der keyboardstring gecleared

function set_input_value(text) {
	value = real(text);
	keyboard_string = "";
	return value
}